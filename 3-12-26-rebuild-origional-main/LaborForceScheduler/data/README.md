# data/
Runtime scheduler data lives here (portable mode).
- `scheduler_data.json` is the primary saved dataset.
- `last_schedule.json` stores last generated schedule for stability.
- `patterns.json` stores learned pattern signals.
- `final_schedules/` keeps published week snapshots.
- `backups/` contains backup zip files.
